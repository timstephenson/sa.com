<%= render 'refinery/admin/search_header', :url => refinery.instructors_admin_instructors_path %>
<div class='pagination_container'>
  <% if @instructors.any? %>
    <%= render 'instructors' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
